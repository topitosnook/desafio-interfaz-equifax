import React from 'react';

export default function Adapters({name}) {
  console.log(name);
   
  return (
    <div>
      <p>{name}..</p>
      <h1>Elegir adaptador</h1>
    </div>
   
        );
}

